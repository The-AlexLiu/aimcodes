---
description: AimCodes React、Canvas、CSS 和移动端界面规则
globs: "src/**/*.{js,jsx,css}"
alwaysApply: false
---

- AimCodes 使用 React + Vite + 原生 CSS；保持现有架构，不为简单功能引入大型 UI 框架。
- 用户路径优先保持“预览 → 调整 → 复制”，避免增加无效入口和重复操作。
- C 端文案简短、自然、像游戏玩家表达；五语种用户可见词条必须同步。
- Canvas 预览与最终复制代码必须一致，颜色变化需要真实改写代码。
- 新增交互必须支持键盘焦点、移动端窄屏和复制失败回退。
- 不使用本机绝对路径，不直接编辑 `dist/`。
- 完成后至少运行 `pnpm lint` 和 `pnpm build`。
