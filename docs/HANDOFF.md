# AimCodes 工具间交接

最后更新：2026-08-07

## 当前交接目标

把 AimCodes 从依赖单一 GPT/Codex 长对话的开发方式，迁移为 GPT/Codex 与 Antigravity 都能从 GitHub 恢复上下文的协作方式。

## 本次已经整理

- 根目录共享智能体规则 `AGENTS.md`；
- 协作流程 `CONTRIBUTING.md`；
- 产品背景、架构、状态、决策、发布和权限文档；
- Antigravity 协作接管验证与固定协议；
- 可共享的团队上下文规范。

## 当前分支与提交

- 主仓库当前分支：`seo/programmatic-pages-v1`
- 整理前最新功能提交：`81cbf20`
- 当前工作树包含用户更早的未提交品牌与配置改动；不要混合处理。

## Antigravity 接手时第一步

1. 先只读，不修改文件。
2. 阅读 `AGENTS.md` 和 `docs/` 中所有 Markdown 文件。
3. 运行 `git status -sb`、`git diff --stat` 和 `git log --oneline -8`。
4. 对照 `docs/CURRENT_STATE.md` 报告差异。
5. 识别哪些修改属于迁移包、哪些属于用户更早的未提交工作。
6. 未获得明确授权前，不提交、不推送、不合并、不部署。

## 尚未完成

- 迁移包尚未提交 GitHub；
- 既有未提交改动尚未按功能拆分；
- 两个草稿 PR 尚未合并；
- Netlify 尚未确认改为 GitHub `main` 自动生产部署；
- 渲染器尚未完成单一源自动同步；
- GA4 事件字典尚未补齐到验证脚本要求的 23 个事件。

## 本次验证结果

共享记忆包创建后已经确认：

- 新增文档与 Antigravity 规则不存在本机绝对路径；
- 未发现 Token、API Key、私钥或其他凭据值；
- `git diff --check` 通过；
- `pnpm lint` 通过；
- 62 条准星源代码、60 种可见样式验证通过；
- 9 个连续反应段位和推荐 ID 验证通过；
- 4 语种词典和 62 条准星本地化验证通过；
- 23 个 GA4 漏斗事件验证通过，未采集原始搜索词；
- Vite 生产构建通过；
- 生成 264 个本地化 HTML 路由和 48 个 sitemap URL；
- SEO 与四语种路由验证通过。

## 下一任务推荐

先执行“建立干净协作基线”，不要直接新增产品功能：

1. 审阅当前所有未提交文件；
2. 把迁移包与历史品牌改动分开暂存；
3. 运行完整验证；
4. 生成清晰提交和 PR；
5. 确认 GitHub、Netlify 与本地版本一致；
6. 再开始 Antigravity 后续开发。

## 交接原则

聊天只用于讨论；长期事实必须回写仓库文档。任务只在可验证的 Git 提交点交接，不在两个工具同时编辑同一工作树时交接。
